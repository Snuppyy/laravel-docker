{
   "version": "0.2.0",
   "configurations": [
      {
         "name": "Launch",
         "type": "php",
         "request": "launch",
         "port": 9003, // default for Xdebug 3
         "log": true,
         // server -> local
         // just the opposite of your bound volumes
         "pathMappings": {
            "/var/www/html/": "${workspaceRoot}/",
         },
      }
   ]
}